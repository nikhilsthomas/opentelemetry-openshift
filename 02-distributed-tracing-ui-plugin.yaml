# This is the CORRECT way to enable distributed tracing UI in OpenShift Console
# Using the proper observability.openshift.io API
apiVersion: observability.openshift.io/v1alpha1
kind: UIPlugin
metadata:
  name: distributed-tracing
spec:
  type: DistributedTracing